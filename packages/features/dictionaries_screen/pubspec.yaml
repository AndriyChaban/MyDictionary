name: dictionaries_screen
publish_to: none

environment:
  sdk: '>=2.19.3 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  dictionary_provider:
    path: ../../dictionary_provider
  user_repository:
    path: ../../user_repository
  components:
    path: ../../components
  domain_models:
    path: ../../domain_models
  flutter_bloc: ^8.1.2
  equatable: ^2.0.5
  file_picker: ^5.2.7
  path:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


flutter:
